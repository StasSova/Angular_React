export class Group {
  constructor(
    public name: string,
    public size: number,
    public faculty: string
  ) {}
}

export class Audience {
  constructor(
    public name: string,
    public capacity: number,
    public faculty: string
  ) {}
}
